#include <limits.h>
#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
    _printf("Let's try to printf a simple sentence. %c %s %% \n", 'a', "test with a ", 1, 3);
    printf("Let's try to printf a simple sentence. %c \n", 2);

    return (0);
}
